// Web-only sessionStorage accessor. Stubbed for non-web platforms via
// the conditional import in platform_router.dart.
String? readSessionStorage(String key) => null;
void removeSessionStorage(String key) {}
